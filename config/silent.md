---
title: silent | 配置
outline: deep
---

# silent <CRoot /> {#silent}

- **类型:** `boolean | 'passed-only'`
- **默认值:** `false`
- **命令行终端:** `--silent`, `--silent=false`

测试过程中控制台保持静默状态。

使用 `'passed-only'` 仅查看失败测试的日志。失败测试的日志会在测试完成后打印。
